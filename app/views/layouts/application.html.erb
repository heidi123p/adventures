<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> Do Something |  <%=  content_for?(:title) ? yield(:title) : "Do Something" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Adventures" %>">
    <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "gmaps.js", "data-turbolinks-track" => true %>
    <%= csrf_meta_tags %>
    <%= favicon_link_tag 'favicon.ico' %>
    <script type="text/javascript" src="http://maps.googleapis.com/maps/api/js?key=AIzaSyCP5NViYBg_el5x_7G8GaKgkxihZig_wvo">
      google.maps.event.addDomListener(window, 'load', initialize);
    </script>
    <%= render 'layouts/shim' %>
  </head>
  <body>
    <header>
      <%= render 'layouts/navigation' %>
    </header>
    <main role="main">
       <%= render 'layouts/messages' %>
       <%= yield %>
       <%= link_to "Sign up now!", signup_path, class: "btn btn-large btn-primary" %>
    </main>
    <%= render 'layouts/footer' %>
  </body>
</html>
